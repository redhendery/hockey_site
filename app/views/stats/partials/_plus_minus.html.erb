<div class="card-deck">
  <div class="card">
    <div class="card-header text-center">
      <strong>Plus / Minus Leaders</strong>
    </div>
    <div class="card-block">
      <% @skate.limit(5).order(plus_minus: :desc).each_with_index do |skate, i| %>
        <div class="table-responsive">
          <span><%= i += 1 %>. </span>
          <span><%= skate.name %></span>
          <span><small class="stats"><%= skate.team.abr %></small></span>
          <span class="float-right"><%= skate.plus_minus %></span>
        </div>
      <% end %>
    </div>
  </div>
</div>
