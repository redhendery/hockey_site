<div class="col-sm-12 col-md-12 col-lg-12 col-xl-12">
  <div class="card-deck">
    <div class="card">
      <div class="card-header">
        <strong>Goal Leaders</strong>
      </div>
      <div class="card-block">
        <% @home.players.limit(3).where(pos_abr: %w[F D]).order(goals: :desc).each_with_index do |goals, i| %>
          <div class="table-responsive">
            <span class="float-left"><%= i += 1 %>. </span>
            <span class="float-left"><%= goals.name %></span>
            <span class="float-right"><%= goals.goals %></span>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
